function Area(name,coordinates,color,description){
    this.id = id
    this.name = name
    this.visible = true
    this.coordinates = coordinates
    this.color = color
    content = "$(description)"
}